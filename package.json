{
  "name": "agent-kanban",
  "private": true,
  "description": "Agent-driven Kanban platform: MCP server, CLI, VS Code extension, and starter templates",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "build:core": "pnpm --filter @agent-kanban/core build",
    "build:vscode": "pnpm run build:core && pnpm --filter agent-kanban-vscode version:bump && pnpm --filter agent-kanban-vscode build && pnpm --filter agent-kanban-vscode package:root"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.32.1"
}